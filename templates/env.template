# OpenClaw 实例环境变量模板
# 实例名称: {{INSTANCE_NAME}}
# 用户: {{USERNAME}}
# 通道类型: {{CHANNEL_TYPE}}
# 创建时间: {{CREATE_TIME}}

# ============================================
# 通道开关配置
# ============================================
FEISHU_ENABLED={{FEISHU_ENABLED}}
DINGTALK_ENABLED={{DINGTALK_ENABLED}}
WECOM_ENABLED={{WECOM_ENABLED}}
TELEGRAM_ENABLED=false
DISCORD_ENABLED=false
SLACK_ENABLED=false

# ============================================
# 飞书配置 (当 FEISHU_ENABLED=true 时填写)
# ============================================
# 在飞书开放平台创建企业自建应用获取
# https://open.feishu.cn/app
FEISHU_APP_ID={{FEISHU_APP_ID}}
FEISHU_APP_SECRET={{FEISHU_APP_SECRET}}
# 连接模式: websocket (推荐) 或 webhook
FEISHU_CONNECTION_MODE=websocket

# ============================================
# 钉钉配置 (当 DINGTALK_ENABLED=true 时填写)
# ============================================
# 在钉钉开放平台创建企业内部应用获取
# https://open-dev.dingtalk.com/
DINGTALK_CLIENT_ID={{DINGTALK_CLIENT_ID}}
DINGTALK_CLIENT_SECRET={{DINGTALK_CLIENT_SECRET}}
# 连接模式: webhook 或 stream
DINGTALK_CONNECTION_MODE=webhook

# ============================================
# 企业微信配置 (当 WECOM_ENABLED=true 时填写)
# ============================================
# 在企业微信管理后台获取
# https://work.weixin.qq.com/wework_admin/frame
WECOM_CORP_ID={{WECOM_CORP_ID}}
WECOM_AGENT_ID={{WECOM_AGENT_ID}}
WECOM_SECRET={{WECOM_SECRET}}

# ============================================
# Gateway 认证配置
# ============================================
# 生成方式: openssl rand -hex 32
# 用于 Control UI 和 API 认证
GATEWAY_TOKEN={{GATEWAY_TOKEN}}

# ============================================
# AI 模型配置
# ============================================
# 可选模型:
# - openrouter/stepfun/step-3.5-flash:free (免费)
# - openrouter/anthropic/claude-3.5-sonnet
# - openrouter/openai/gpt-4o
# - anthropic/claude-3.5-sonnet (需要 ANTHROPIC_API_KEY)
# - openai/gpt-4o (需要 OPENAI_API_KEY)
MODEL_NAME={{MODEL_NAME}}

# 如果使用需要 API Key 的模型，取消注释并填写:
# ANTHROPIC_API_KEY=your-anthropic-key
# OPENAI_API_KEY=your-openai-key

# ============================================
# 高级配置 (通常不需要修改)
# ============================================
# 日志级别: error, warn, info, debug
LOG_LEVEL=info

# 会话隔离模式: per-peer (每个对话独立)
DM_SCOPE=per-peer

# 最大并发会话数
MAX_CONCURRENT_SESSIONS=50
